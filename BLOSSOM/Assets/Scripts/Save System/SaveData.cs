using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveData : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // progress checks

    bool powersActivated;

    bool nuckelaveeDead;
    bool bugbearDead;
    bool nimuehDead;
    bool unit_1023Dead;

    // player character stats

    void Load() // called when save file is loaded
    {

    }

    void Save() // called when game is saved
    {

    }
}
